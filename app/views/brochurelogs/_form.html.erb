<%= form_for(@brochurelog) do |f| %>
  <% if @brochurelog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brochurelog.errors.count, "error") %> prohibited this brochurelog from being saved:</h2>

      <ul>
      <% @brochurelog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label "Amount handed" %>
    <%= f.number_field :count %>
  </div>
  <div class="field">
	<%= f.label "Facility" %>
  	<%= collection_select(:brochurelog , :facility_id, Facility.all , :id, :name) %>
  </div>
  
  <div class="field">
	<%= f.label "Brochure" %>
  	<%= collection_select(:brochurelog , :brochure_id, Brochure.all , :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit "Create Brochure Record"%>
  </div>
<% end %>
