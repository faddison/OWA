<%= form_for(@referrallog) do |f| %>
  <% if @referrallog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@referrallog.errors.count, "error") %> prohibited this referrallog from being saved:</h2>

      <ul>
      <% @referrallog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.text_field :date %>
  </div>
  <div class="field">
    <%= f.label :count %><br />
    <%= f.number_field :count %>
  </div>
  <div class="field">
    <%= f.label :facility %><br />
    <%= f.text_field :facility %>
  </div>

<div class="field">
	<%=f.label "leos" %><br/>
	<%=collection_select(:Referral,:referrals,Referral.all,:id,:name)%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
