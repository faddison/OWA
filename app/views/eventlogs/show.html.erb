<p id="notice"><%= notice %></p>

<p>
  <b>Event:</b>
  <%= @eventlog.event.name %>
</p>

<p>
  <b>Visitor:</b>
  <%= @eventlog.visitor.name %>
</p>

<p>
  <b>Date:</b>
  <%= @eventlog.event.date %>
</p>

<%= link_to 'Edit', edit_eventlog_path(@eventlog) %> |
<% if can? :update, Eventlog %>
	<%= link_to 'Back', eventlogs_path %>
<% else %>
	<%= link_to 'Back', home_path %>
<% end %>