<!DOCTYPE html>
<html>
<head>
  <title>Options</title>
  	<%= stylesheet_link_tag    "application", :media => "all" %>
  	<%= javascript_include_tag "https://js.stripe.com/v1/" %>
  	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
  
  <style>
			body {
				padding-top: 60px;
			}
			.hero-unit{
				height:300px;
			}	
			.carousel{
				width:500px;
				float:right;
			}	
		</style>
  
  
  <script>
// Load this when the DOM is ready
$(function(){
  // You used .myCarousel here. 
  // That's the class selector not the id selector,
  // which is #myCarousel
  $('#myCarousel').carousel();
});
</script>
  
</head>
<body>

<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<a class="brand" href="/">Options</a>
				<div class="navbar-form pull-right">
  					<div class="btn btn-primary">
						<div id="user_nav">
							<% if user_signed_in? %>
								Signed in as <%= current_user.email %>. Not you?
								<%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
							<% else %>
								<%= link_to "Sign In", new_user_session_path %>
							<% end %>
						</div>
					</div>
				</div>
			
			<br>
 			
 			<div class="nav-collapse">
            	<ul class="nav">
    				<li><%= link_to "Visitors", visitors_path %></li>
        			<li><%= link_to "Facilities", facilities_path %></li>
              
              		<li class="dropdown">
    					<a class="dropdown-toggle" data-toggle="dropdown">
      						<%= "Events" %>
      						<b class="caret"></b>
    					</a>
    					<ul class="dropdown-menu">
      						<li><%= link_to "Events", events_path %></li>
        					<li><%= link_to "Event Type", eventtypes_path %></li>
        					<li><%= link_to "Event Records", eventlogs_path %></li>
    					</ul>
  					</li>
              
              		<li><%= link_to "Programs", programs_path %></li>
              		
              		<li class="dropdown">
    					<a class="dropdown-toggle" data-toggle="dropdown">
      						<%= "Brochures" %>
      						<b class="caret"></b>
    					</a>
    					<ul class="dropdown-menu">
      						<li><%= link_to "Brochures", brochures_path %></li>
    						<li><%= link_to "Brochure Records", brochurelogs_path %></li>
    					</ul>
  					</li>
    				
    				<li class="dropdown">
    					<a class="dropdown-toggle" data-toggle="dropdown">
      						<%= "Referrals" %>
      						<b class="caret"></b>
    					</a>
    					<ul class="dropdown-menu">
      						<li><%= link_to "Referrals", referrals_path %></li>
    						<li><%= link_to "Referral Records", referrallogs_path %></li>
    					</ul>
  					</li>
    				
    				<li><%= link_to "Users", users_path %>
              	</ul> 
    		</div>
    		
		</div>
	</div>
</div>

<br>

<div class="container">
     		
	<br>
	<br>
 			
	<% flash.each do |name, msg| %>
		<%= content_tag :div, msg, :id => "flash_#{name}" %>
	<% end %>

<%= yield %>
	<p class="notice"><%= notice %></p>
	<p class="alert"><%= alert %></p>
 
</div>

<br>
 
</body>
</html>
